specVersion: "v1beta"
serviceId: "quoting-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "quoting-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
